import React from 'react';
import {View, StyleSheet,Text} from 'react-native';

const Card = () => {
    return (
        <View>
           <Text>Card</Text> 
        </View>
    );
}

const styles = StyleSheet.create({})

export default Card;
